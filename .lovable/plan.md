

# Plan: Enhanced Bespoke Footer & Navigation Arrival Prompt

This is a prompt-engineering task, not a code implementation task. You are asking me to take your existing footer/nav arrival prompt and make it significantly more in-depth, detailed, and brand-specific so you can copy-paste it into Lovable.dev for implementation.

After reviewing the current state of all three components (`Footer.tsx`, `MinimalHeader.tsx`, `MobileStickyBar.tsx`) and the CSS animations, here is what I will produce:

## What the Enhanced Prompt Will Include (Beyond What You Already Have)

1. **Exact current code state references** — line-by-line references to what exists now so the AI knows precisely what to preserve vs. enhance
2. **The "Ceremony Completion" arrival choreography** — a frame-by-frame specification of what happens when the header meets the footer, written as a musical score with exact millisecond timings
3. **Piano-themed micro-interactions that are not literal** — the "key depression" hover behavior on nav links (subtle 1px downward shift like pressing a piano key), the "pedal sustain" on the golden thread breathing, the "recessional tempo" of the staggered reveal
4. **Wedding-themed atmospheric shifts** — the footer warming like candlelight as the visitor reaches the bottom, the grain intensifying like the texture of invitation paper, the vow-yellow fog pooling like evening light through stained glass
5. **The "Empty Venue" emotional temperature** — specific CSS values for the atmospheric layers that create the feeling of being alone in a beautiful room after everyone has left
6. **Header-Footer "conversation" specification** — exact behaviors for how the two components acknowledge each other during arrival, including synchronized breathing animations, mirrored golden threads, and the vertical axis alignment of "Parker Gawryletz" (header) with "'Til Death ; Unto Life." (footer)
7. **Mobile arrival simplification** — exact mobile-specific behavior including the MobileStickyBar fade-out coordination
8. **Anti-pattern expansion** — 25+ specific things to never do, with brand-reasoning for each
9. **CSS keyframe specifications** — exact animation definitions needed, referencing the existing `footer-breathe` (8s cycle) and `semicolon-heartbeat` (2s cycle) keyframes
10. **The "Three Tempos" polyrhythm** — 2s heartbeat, 3s dot breath, 4s/8s frame breath running simultaneously like instruments in different time signatures

## Deliverable

A single, complete, copy-paste-ready prompt that is approximately 2-3x the length of your current prompt, with every brand detail, every CSS value, every animation timing, and every interaction behavior specified to the level where the implementing AI has zero ambiguity.

Shall I proceed with generating this enhanced prompt?


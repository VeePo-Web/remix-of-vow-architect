

# Plan: Write the Definitive Bespoke Footer & Navigation Arrival Prompt

This is a prompt-engineering task. I will overwrite `.lovable/plan.md` with the most comprehensive, detail-obsessive version of this prompt yet — incorporating the exact current code state I just reviewed, and adding layers of specificity around the visual overhaul, piano/wedding thematic integration, and the header-footer "arrival" easter egg.

## What Changes from the Previous Version

The current `.lovable/plan.md` (766 lines) already contains significant detail. The enhanced version will:

1. **Add a "Visual Overhaul Directives" section** — specific instructions for what to change visually in the footer UI/UX (the user's core request this time: "Do a full overhaul visually on the UI and UX of the footer" / "quality, extreme detail orientated")
2. **Expand the piano/wedding thematic layer** — more specific non-literal implementations (the "key depression" hover, "pedal sustain" breath, "recessional tempo" reveals, "empty venue" atmospheric temperature)
3. **Add a "Current State vs. Target State" diff** — so the implementing AI knows exactly what exists and what must change
4. **Add CSS keyframe specifications** — exact `@keyframes` definitions for new animations (`golden-dot-breathe` at 3s, enhanced `footer-breathe` sync)
5. **Strengthen the arrival choreography** — a musical score format with measures, beats, and exact millisecond timings
6. **Add the "Three Tempos" polyrhythm specification** — 2s heartbeat, 3s dot, 8s frame breathing simultaneously
7. **Expand anti-patterns to 25+** with brand-specific reasoning

## File to Edit

- `.lovable/plan.md` — full overwrite with the enhanced prompt (~900-1000 lines)

## No Other Files Changed

This is a prompt document, not code. No components are modified.


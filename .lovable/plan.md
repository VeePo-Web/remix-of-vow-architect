

# Plan: Generate Enhanced Bespoke Footer & Navigation Arrival Prompt

This is a **prompt-engineering deliverable**, not a code change. I will write the massively enhanced prompt to `.lovable/plan.md` — a single copy-paste-ready document.

## What I Have Reviewed
- `Footer.tsx` (314 lines) — current footer with arrival state, atmospheric layers, recessional stagger, covenant bookend
- `MinimalHeader.tsx` (219 lines) — arrival easter egg with logo centering, nav fade, golden thread sync
- `MobileStickyBar.tsx` (99 lines) — footer bookend IntersectionObserver for fade-out
- `index.css` — `footer-breathe` (8s cycle), `semicolon-heartbeat` (2s cycle), `footer-fade-bridge` (60px gradient)

## What the Enhanced Prompt Will Add Over Your Current Version

1. **Exact current code state** — verbatim JSX snippets and CSS keyframes so the implementing AI knows precisely what exists and what to preserve vs. enhance
2. **"Ceremony Completion" arrival score** — frame-by-frame millisecond choreography written as a musical score (measure 1: nav links dissolve at 80ms stagger, measure 2: logo glides center at 260ms, measure 3: underline draws at 450ms, etc.)
3. **Piano-themed non-literal interactions** — "key depression" hover (1px translateY on nav links), "pedal sustain" golden thread breathing, "recessional tempo" stagger reveals
4. **Wedding-themed atmospheric shifts** — specific CSS values for warmth pooling during arrival (grain 6%→8%, fog 1.5%→3%, dot glow triple-layer intensification)
5. **The "Empty Venue" emotional temperature** — exact atmospheric layer values that create the feeling of an empty room after the ceremony
6. **Header-Footer "conversation"** — synchronized breathing (both golden threads on same `footer-breathe` 8s phase), vertical axis alignment spec, mirrored opacity values
7. **The Three Tempos polyrhythm** — 2s semicolon heartbeat, 3s golden dot ambient pulse, 8s frame breath — three time signatures running simultaneously
8. **Mobile arrival specification** — sticky bar fade coordination, simplified header behavior
9. **25+ anti-patterns** with brand-specific reasoning
10. **CSS keyframe definitions** — exact `@keyframes` for any new animations needed
11. **Visual overhaul directives** — specific instructions for the full UI/UX redesign of the footer while preserving the IA and brand identity

## Deliverable
Single file: `.lovable/plan.md` — approximately 2-3x the length of the current prompt, with zero ambiguity for the implementing AI. Every CSS value, every timing, every interaction behavior specified.

## Implementation
One task: overwrite `.lovable/plan.md` with the enhanced prompt document.

